# Changelog

## [1.1.1] - 2026-08-29

### 🖼️ 移动端
- **首页文章卡封面随机化**：同一文章在同一 2 小时窗口内固定同一张（稳定不闪变），跨窗口自动更换随机图；窗口内不同文章封面互不重复（优先分配未占用图，图库用尽才允许复用）
- **资源推荐卡封面同步**：推荐横滑卡片使用与首页一致的随机封面逻辑（独立图池分配，互不重复）
- **图库缺失兜底**：`photoFinder` 不存在（photo 插件未启用）时不再报模板渲染异常，首页 / 资源页正常访问

### 🧭 交互
- **文章详情返回恢复位置**：文章卡片进详情后返回恢复原列表滚动位置，返回瞬间禁用入场动画消除闪烁
- **刷新 / 普通跳转回顶**：关闭浏览器默认滚动恢复，刷新页面与 tabbar 跳转均回到顶部，不残留旧位置

### 🎨 封面与卡片
- **首页文章卡封面完整展示**：封面图改为按自身比例完整显示（不再固定高度裁切）
- **卡片间距压缩**：文章简介到阅读量的间距从 20px 压缩到 4px，行距收紧，卡片更紧凑
- **简介单行**：移动端文章简介单行显示，卡片更整齐

## [1.1.0] - 2026-08-29


### 📱 移动端体验
- **底部 Tabbar 液态玻璃化**：底部导航改为单层液态玻璃大胶囊（毛玻璃 + 渐变 + 圆角），选中项以灰色玻璃高亮铺满对应格、无边框无嵌套感，深浅色模式各自适配
- **移动端页脚间距统一**：移除 flex:1/margin-top:auto 撑底逻辑，所有页面页脚紧贴正文、间距一致
- **首屏内容更多**：资源 / 工具箱 / 我的（关于）顶部 hero 压缩（标题/logo/间距收缩），首屏露出更多真实内容
- **首页文章卡移动端紧凑化**：封面高度从 180→160→112px，标题/内边距收紧，一屏可展示更多卡片
- **tabbar 公共变量**：将液态玻璃渐变、胶囊圆角、选中色等重复硬编码收敛为 `--tab-*` / `--glass-*` 主题变量
- **系统偏好适配**：全部动效适配 `prefers-reduced-motion`

### ✨ 新特性
- **布局契约重构**：创建根级 `templates/layout.html`，全站 17 个页面统一改用 `~{layout :: html(...)}` 引用，符合 Halo 2.19+ 根布局约定
- **分类切换改为「下划线 Tab」**：首页 / 资源中心 / 工具箱的分类切换统一为应用商店风格 —— 顶部文字 + 选中项蓝色下划线动画，横向可滚动
- **分类栏吸顶固定**：三处分类/筛选栏 `position: sticky` 吸顶（带背景色），滚动时下方内容独立滑动
- **分类 Tab 字号提升**：切换 12→14→16px，与内容标题同级、更醒目
- **横滑卡片优化**：首页热文 / 资源推荐卡移动端重新收窄并右侧渐隐，一眼可滑动；滑到底最后一张完整显示
- **全局字号变量**：引入 `--fs-page-title / --fs-section-title / --fs-card-title / --fs-card-desc / --fs-meta / --fs-filter`，统一资源中心、工具箱、首页三处聚合页的标题与字体层级，移动端自动降档
- **资源推荐卡恢复阅读量显示**（热度标识）

### 🎨 Apple 化（设计体系）
- 文章详情页 / 资源详情页完成 Apple 化（渐变、毛玻璃、pill、入场动画、spring 交互）
- 弹窗搜索（SearchModal）Apple 化
- 修复按压态 / 焦点圆角 bug

### 🐛 修复
- 修复横滑卡「滑动后第一次点击失效」：移除 `setPointerCapture` 与冗余 click 拦截
- 修复首页推荐末张卡滑底被裁剪
- bfcache 滚动恢复逻辑：卡片返回恢复位置、Tab 切换回顶部

### 🧹 清理
- 删除未使用的模板 `post_resource.html`、`category_resources.html` 及对应 `theme.yaml` 注册

### 📦 安装
1. 下载 `theme-aiym-v1.1.0.zip`
2. Halo 后台 → 外观 → 主题 → 安装主题 → 上传 zip
3. 启用主题
4. 升级已有安装：后台「重载主题」即可刷新样式缓存